#pragma once

// Increment for every published editor build. LIVE_PROTOCOL changes only when
// the wire format is incompatible; patch/minor engine releases may still share
// the same live protocol.
#define IMPULSO_ENGINE_VERSION "0.2.0"
#define IMPULSO_LIVE_PROTOCOL 2u

